<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="SG_RECU_AdolfoRodri.MVVM.Views.ListaTareasView"
             xmlns:conv="clr-namespace:SG_RECU_AdolfoRodri.Converters"
             xmlns:local="clr-namespace:SG_RECU_AdolfoRodri.MVVM.ViewModels"
             NavigationPage.HasNavigationBar="False">
    <ContentPage.Resources>
        <ResourceDictionary>
            <conv:BoolToStringConverter x:Key="EstadoConverter"/>
        </ResourceDictionary>
    </ContentPage.Resources>

    <Grid RowDefinitions="Auto,*">
        <CollectionView ItemsSource="{Binding Tareas}" SelectionMode="Single" SelectedItem="{Binding TareaSeleccionada}">
            <CollectionView.ItemTemplate>
                <DataTemplate>
                    <Grid Padding="10" ColumnDefinitions="Auto,*,Auto" RowDefinitions="Auto" BackgroundColor="BlueViolet" Margin="0,5">
                        <Button Text="✅"
                                Command="{Binding Source= {RelativeSource AncestorType= {x:Type local:ListaTareasViewModel}}, Path=CambiarEstadoCommand}"
                                BackgroundColor="Pink" FontSize="12"
                                WidthRequest="80" HeightRequest="40" CornerRadius="10" VerticalOptions="Center">
                        </Button>
                        <StackLayout Grid.Column="1" Padding="10" Spacing="2">
                            <Label Text="{Binding Titulo}" FontSize="16" FontAttributes="Bold"/>
                            <Label Text="{Binding Descripcion, StringFormat='{}{0:20}'}" FontSize="13"/>
                            <Label Text="{Binding Estado, Converter={StaticResource EstadoConverter}}" FontSize="13"/>
                            <Label Text="{Binding Etiquetas, Converter={StaticResource EtiquetasConverter}}" FontSize="12"/>
                        </StackLayout>
                        <Button Grid.Column="2"
                                Text="EDITAR" TextColor="White"
                                WidthRequest="80" HeightRequest="40" CornerRadius="10" VerticalOptions="Center"
                                BackgroundColor="Pink"
                                Command="{Binding Source={RelativeSource AncestorType={x:Type local:ListaTareasViewModel}}, Path=EditarTareaCommand}">

                        </Button>
                    </Grid>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>
        <Button Grid.Row="1" Text="+" 
                WidthRequest="60" HeightRequest="60" CornerRadius="30"
                VerticalOptions="End" HorizontalOptions="Center"
                Command="CrearTareaCommand"/>
    </Grid>
</ContentPage>