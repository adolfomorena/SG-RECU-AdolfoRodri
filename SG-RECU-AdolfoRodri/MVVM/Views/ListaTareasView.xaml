<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="SG_RECU_AdolfoRodri.MVVM.Views.ListaTareasView"
             xmlns:conv="clr-namespace:SG_RECU_AdolfoRodri.Converters"
             xmlns:local="clr-namespace:SG_RECU_AdolfoRodri.MVVM.ViewModels"
             NavigationPage.HasNavigationBar="False">

    <ContentPage.Resources>
        <ResourceDictionary>
            <conv:BoolToStringConverter x:Key="EstadoConverter"/>
        </ResourceDictionary>
    </ContentPage.Resources>

    <Grid>
        <CollectionView ItemsSource="{Binding Tareas}"
                        SelectionMode="Single"
                        SelectedItem="{Binding TareaSeleccionada}">
            <CollectionView.ItemTemplate>
                <DataTemplate>
                    <Grid Padding="10" ColumnDefinitions="Auto,*,Auto,Auto" RowDefinitions="Auto" BackgroundColor="#303030" Margin="0,5">
                        <Button Text="✅"
                                Command="{Binding Source={RelativeSource AncestorType={x:Type local:ListaTareasViewModel}}, Path=CambiarEstadoCommand}"
                                BackgroundColor="DarkOliveGreen" FontSize="12"
                                WidthRequest="80" HeightRequest="40" CornerRadius="10" VerticalOptions="Center"/>

                        <HorizontalStackLayout Grid.Column="1" Padding="10" Spacing="20">
                            <Label Text="{Binding Titulo}" FontSize="16" FontAttributes="Bold" TextColor="White"/>
                            <Label Text="{Binding Descripcion, StringFormat='{}{0:20}'}" FontSize="13" TextColor="White"/>
                            <Label Text="{Binding Estado, Converter={StaticResource EstadoConverter}}" FontSize="13" TextColor="White"/>
                            <CollectionView ItemsSource="{Binding Etiquetas}" ItemsLayout="Horizontal" Grid.Column="4"  Margin="0,5,0,0">
                                <CollectionView.EmptyView>
                                    <Grid>
                                        <VerticalStackLayout>
                                            <Label HorizontalTextAlignment="Center" Text="No hay etiquetas" TextColor="White" FontAttributes="Bold"/>
                                        </VerticalStackLayout>
                                    </Grid>
                                </CollectionView.EmptyView>
                                <CollectionView.ItemTemplate>
                                    <DataTemplate>

                                        <HorizontalStackLayout>
                                            <Label Text="{Binding Nombre}" TextColor="White"/>
                                            <Label Text=", "/>
                                        </HorizontalStackLayout>    
                                    </DataTemplate>
                                </CollectionView.ItemTemplate>
                            </CollectionView>
                        </HorizontalStackLayout>

                        <Button Grid.Column="2"
                                Text="EDITAR" TextColor="White"
                                WidthRequest="80" HeightRequest="40" CornerRadius="10" VerticalOptions="Center"
                                BackgroundColor="Pink"
                                Command="{Binding Source={RelativeSource AncestorType={x:Type ContentPage}}, Path=EditarTareaCommand}"
                         
                                />
                        <Button Grid.Column="3"
                        Text="Borrar" TextColor="White"
                        WidthRequest="80" HeightRequest="40" CornerRadius="10" VerticalOptions="Center"
                        BackgroundColor="Red"
                        Command="{Binding Source={RelativeSource AncestorType={x:Type ContentPage}}, Path=BorrarTareaCommand}"
                        />

                    </Grid>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>
        <Button Text="+"
                WidthRequest="60" HeightRequest="60" CornerRadius="30"
                BackgroundColor="DarkOrange" TextColor="White"
                HorizontalOptions="Center" VerticalOptions="End"
                Margin="20"
                Command="{Binding CrearTareaCommand}"/>
    </Grid>
</ContentPage>
