<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="SG_RECU_AdolfoRodri.MVVM.Views.GestionEtiquetasView"
             Title="GestionEtiquetasView">
    <VerticalStackLayout>

        <Button Text="Volver" Command="{Binding VolverCommand}"/>


        <Entry Placeholder="Etiqueta" Text="{Binding EtiquetaSeleccionada.Nombre}" TextChanged="Entry_TextChangued"/>
        <Button Text="Guardar" Command="{Binding GuardarEtiquetaCommand}"/>
        <Button Text="Eliminar" Command="{Binding EliminarEtiquetaCommand}"/>


        <VerticalStackLayout>
            <CollectionView Grid.Row="1" ItemsSource="{Binding ListaEtiquetas}"
                  SelectionMode="Single"
                  SelectedItem="{Binding EtiquetaSeleccionada}">

                <CollectionView.EmptyView>
                    <Grid>
                        <VerticalStackLayout>
                            <Label HorizontalTextAlignment="Center" Text="No hay etiquetas" FontSize="Large" FontAttributes="Bold"/>
                        </VerticalStackLayout>
                    </Grid>
                </CollectionView.EmptyView>


                <CollectionView.ItemTemplate>
                    <DataTemplate>
                        <Frame Padding="0" Margin="5,0">
                            <Label Text="{Binding Nombre}"/>
                        </Frame>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>

        </VerticalStackLayout>
    </VerticalStackLayout>
</ContentPage>